my pub key not copied to instance
update-dns doesnt find aws credentials
the sh script should also have the domain and record names set
The generated sh script:

#!/bin/bash ?????

export AWS_ACCESS_KEY_ID=***
export AWS_SECRET_ACCESS_KEY=***
export DOMAIN_NAME=kominik.net
export RECORD_NAME=vault
